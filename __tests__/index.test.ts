/* This is a test that is testing the math function. */
describe('Server.ts tests', () => {
    /* This is a test that is testing the math function. */
    test('Math test', () => {
        expect(2 + 2).toBe(4);
    });
});
